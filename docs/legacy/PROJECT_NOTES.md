# Midway Mobile Storage Project Notes

## Deployment
Deployed on GoDaddy cPanel (forced to use PHP backend instead of Node).

## Structure
- `/frontend/` - React app (active)
- `/backend/` - Active backend (GoDaddy cPanel deployment)
- `/backend/` - Unused Node backend (kept for reference/future migration)

## Status
Deployed and live. No active development.
